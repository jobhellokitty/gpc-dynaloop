# GPC-DynaLoop 工程规则

## 自动提交与推送标准

完成一个边界清晰的任务后，只有同时满足以下条件才自动提交并推送：

1. 用户要求的功能已经完成，不提交半成品或仅有计划的改动。
2. 相关测试全部通过；无法执行的测试必须明确说明，且不得自动推送。
3. `git diff --check` 通过。
4. 已检查待提交内容，不包含密码、私钥、访问令牌或其他凭据。
5. 不提交数据集、模型权重、检查点、缓存、日志和生成结果。
6. 单个新增文件不超过 10 MiB；确需提交大文件时先征得用户确认。
7. 仅按明确路径暂存本任务产生的文件，不使用 `git add .` 或 `git add -A`。
8. 工作区存在无关修改时保留不动；无法与本任务安全分离时停止自动推送。
9. 只允许普通快进推送到当前分支，禁止 force push、重写历史或跳过 Git hooks。
10. 推送后核对本地 `HEAD` 与远程分支提交一致。

满足以上标准视为用户对普通 `commit` 和 `push` 的持续授权。任何未满足项都停止自动推送并向用户报告。

## 仓库边界

- 代码与小型配置保存在本仓库。
- 大文件保存在 `/root/pc/gpc_dynaloop_storage`。
- 不修改 `/root/pc/Embodied-Omni` 上游参考代码；需要复用时在本仓库内实现适配层。
